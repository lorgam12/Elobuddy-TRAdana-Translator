using EloBuddy;
using EloBuddy.SDK;
using System;
using System.Linq;
using EloBuddy.SDK.Enumerations;

namespace ReRyze.Utility
{
    public static class UltimateTeleport
    {
        public static void Execute()
        {
            /*int allies = EntityManager.Heroes.Allies.Where(ally => ally.IsInRange(ally, 425) && !ally.IsDead).Count();
            int enemies = EntityManager.Heroes.Enemies.Where(enemy => enemy.IsInRange(enemy, SpellManager.R.Range) && !enemy.IsDead).Count();

            if (allies < ConfigList.UltimateTeleport.MinAllyToTP || enemies < ConfigList.UltimateTeleport.MinEnemyToTP)
                return;
             * 
            */
        }
    }
}
